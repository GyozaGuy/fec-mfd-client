{
  "name": "fec-commlink-server",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "build": "rimraf dist && electron-packager . --platform=win32 --arch=x64 --icon=fec --out=dist",
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^4.0.6",
    "electron-packager": "^13.1.0",
    "rimraf": "^2.6.3"
  },
  "dependencies": {
    "node-key-sender": "^1.0.11",
    "node-localstorage": "^1.3.1",
    "ws": "^6.1.4"
  }
}
